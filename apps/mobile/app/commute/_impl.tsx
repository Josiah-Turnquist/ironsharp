// Commute Mode requires a native dev build (uses @react-native-voice/voice).
// This stub keeps Expo Go from crashing on module load.
// When ready: replace this file with the full implementation from git history.
export default function CommuteImpl() { return null; }
